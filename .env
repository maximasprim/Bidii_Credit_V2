# Copy to .env and adjust if the backend runs somewhere other than localhost:8000
VITE_API_URL=http://localhost:8000

# Required for the Branch Locator map (src/components/ui/BranchMap.tsx).
# Create a key in Google Cloud Console with the "Maps JavaScript API" enabled
# and billing turned on (required by Google even to stay within the free tier),
# then restrict the key to your domain(s).
VITE_GOOGLE_MAPS_API_KEY=AIzaSyCO_1CcYrwG3jyi9HLiPID5wCF8JUO5Y54
VITE_CRM_API_URL=http://localhost:8000